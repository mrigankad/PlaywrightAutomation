/**
 * Plugin exports
 *
 * This module exports all built-in plugins for the automation tool.
 *
 * @example
 * ```typescript
 * import { RegexExtractor, OutlookProvider } from '@email-automation/core/plugins';
 * ```
 */

export { RegexExtractor, createRegexExtractor, defaultPatterns } from './regex-extractor';
